import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-gestao-medicamentos',
  webDir: 'www'
};

export default config;
